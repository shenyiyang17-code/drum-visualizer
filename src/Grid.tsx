import React from "react";

export default function Grid() {
  return (
    <div
      style={{
        margin: 40,
        padding: 40,
        background: "red",
        color: "yellow",
        border: "6px solid #00FFFF",
        fontSize: 32,
        fontWeight: "bold",
        textAlign: "center",
      }}
    >
      GRID FILE IS ACTIVE — FORCE TEST
    </div>
  );
}
